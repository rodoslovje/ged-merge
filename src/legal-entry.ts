// Entry for the standalone /privacy and /terms pages (and their Slovenian
// slugs /zasebnost and /pogoji) — static markup, same shared styling as
// /guide and /changelog (see guide-entry.ts), no React needed.
import "./theme/fonts.css";
import "./theme/heritage-pine.css";
import "./index.css";
import "./theme/components.css";
import "./guide.css";
