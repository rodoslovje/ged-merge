// Entry for the standalone /guide page — static, crawlable HTML that shares
// the app's theme/fonts so it looks identical to the in-app help, without
// bundling React (the page is plain markup, no interactivity needed).
import "./theme/fonts.css";
import "./theme/heritage-pine.css";
import "./index.css";
import "./theme/components.css";
import "./guide.css";
